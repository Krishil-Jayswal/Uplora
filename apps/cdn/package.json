{
  "name": "cdn",
  "version": "1.0.0",
  "description": "A proxy server for handling dynamic path requests.",
  "license": "ISC",
  "author": "Krishil Jayswal",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "nodemon --config ../../packages/nodemon-config/nodemon.json",
    "build": "tsc -b",
    "start": "node dist/index.js",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/nodemon-config": "*",
    "@repo/typescript-config": "*",
    "@types/express": "^5.0.3",
    "@types/http-proxy": "^1.17.16"
  },
  "dependencies": {
    "@repo/db": "*",
    "@repo/env": "*",
    "express": "^5.1.0",
    "http-proxy": "^1.18.1"
  }
}
