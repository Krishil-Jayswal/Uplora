{
  "name": "socket",
  "version": "1.0.0",
  "description": "A websocket server for streaming events in realtime.",
  "license": "ISC",
  "author": "Krishil Jayswal",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "nodemon --config ../../packages/nodemon-config/nodemon.json",
    "build": "tsc -b",
    "start": "node dist/index.js",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/nodemon-config": "*",
    "@repo/typescript-config": "*",
    "@types/ws": "^8.18.1"
  },
  "dependencies": {
    "@repo/db": "*",
    "@repo/env": "*",
    "@repo/jwt": "*",
    "@repo/redis": "*",
    "@repo/validation": "*",
    "ws": "^8.18.3"
  }
}
