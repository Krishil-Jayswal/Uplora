{
  "name": "processor",
  "version": "1.0.0",
  "description": "A consumer for managing deployment jobs.",
  "license": "ISC",
  "author": "Krishil Jayswal",
  "type": "module",
  "scripts": {
    "dev": "nodemon --config ../../packages/nodemon-config/nodemon.json",
    "build": "tsc -b",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@kubernetes/client-node": "^1.3.0",
    "@repo/env": "*",
    "@repo/kafka": "*",
    "@repo/validation": "*"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/nodemon-config": "*",
    "@repo/typescript-config": "*"
  }
}
