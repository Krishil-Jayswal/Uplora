{
  "name": "worker",
  "version": "1.0.0",
  "description": "A worker for doing the deployment job.",
  "license": "ISC",
  "author": "Krishil Jayswal",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "npm run build && npm run start",
    "build": "tsc -b",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/nodemon-config": "*",
    "@repo/typescript-config": "*"
  },
  "dependencies": {
    "@azure/storage-blob": "^12.27.0",
    "@octokit/auth-app": "^8.0.2",
    "@repo/env": "*",
    "@repo/kafka": "*",
    "@repo/redis": "*",
    "@repo/validation": "*",
    "mime": "^4.0.7",
    "simple-git": "^3.28.0",
    "strip-ansi": "^7.1.0"
  }
}
