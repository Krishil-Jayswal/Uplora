{
  "name": "api",
  "version": "1.0.0",
  "description": "A http server for handling user data.",
  "license": "ISC",
  "author": "Krishil Jayswal",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "nodemon --config ../../packages/nodemon-config/nodemon.json",
    "build": "tsc -b",
    "start": "node dist/index.js",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/nodemon-config": "*",
    "@repo/typescript-config": "*",
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.1"
  },
  "dependencies": {
    "@octokit/app": "^16.0.1",
    "@repo/jwt": "*",
    "@repo/db": "*",
    "@repo/env": "*",
    "@repo/kafka": "*",
    "@repo/redis": "*",
    "@repo/validation": "*",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
